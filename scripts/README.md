# Usage

## commands

```shell
# sudo apt install miniupnpc -y
# upnpc -a 192.168.0.50 22 9000 TCP
# upnpc -l 
# upnpc -d 9000 TCP
curl https://raw.githubusercontent.com/user00000001/jayb/master/scripts/init.sh | REMOTE_IP=111.111.111.111 REMOTE_PORT=8880 bash
```